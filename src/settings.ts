export const SETTINGS = {
  autocompleteWhenPossible: true, // when all tiles have only 1 possible solution
  removeFullyUsedNumber: true, // remove numbers from numeric tooltip when they have been used 9 times
  indicateSameNumber: true, // add overlay to those numbers that are same as one in focus
};
